# Dream Incorporated illustrations

The park background is decorative scenery adapted from the mock's SVG. The
coin star, department/tool icons and upgrade arrow are isolated SVG elements
from `mocks.html`; their adjacent PNGs are transparent renders. Text, progress
arcs, project state, hit targets and selection remain native widgets.
