# Cozy Cafe icons

The flower, rating stars, clock and three tool icons are isolated SVG elements
from the Cozy Cafe design in `mocks.html`. PNGs are transparent 400px renders
of the adjacent SVG sources. All text, controls and changing values are drawn
by the native screen.
