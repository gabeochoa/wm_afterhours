# Fighter lobby scenery

`lobby.svg` is decorative scenery adapted from the fighter mock: room, floor,
gears and avatar. `lobby.png` renders that source without UI labels or controls.
The native screen draws its menu, selection, hologram and descriptions on top.

The menu icons and cog are isolated SVG elements from `mocks.html`, rendered
as transparent PNGs. All labels remain native.
