# Neon Strike HUD illustrations

Each SVG is isolated from the Neon Strike design in `mocks.html`, with all text
removed. Adjacent PNGs are rendered at twice the native 720p dimensions.
They contain atmosphere, icons and minimap scenery. Labels, status, selection,
ammunition and hit targets remain native widgets.
